<% ref_references.each do |name, att| -%>
      .col-md-6= render 'form_nests', f: f, nested: '<%= name %>', ref_chain: '<%= "#{ref_chain.join('.')}.#{att.name}" %>', has_one: <%= att.has_one? %>
<% end -%>
